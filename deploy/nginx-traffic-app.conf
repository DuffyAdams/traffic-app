server {
    listen 80;
    listen [::]:80;
    server_name traffic-app.duffyadams.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://traffic-app.duffyadams.com$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name traffic-app.duffyadams.com;

    ssl_certificate /etc/letsencrypt/live/traffic-app.duffyadams.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/traffic-app.duffyadams.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    client_max_body_size 25m;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;

    location /assets/ {
        alias /home/ubuntu/projects/san-diego-traffic-watch/traffic-app/dist/assets/;
        add_header Cache-Control "public, max-age=2592000, immutable";
    }

    location /map_tiles/ {
        alias /home/ubuntu/projects/san-diego-traffic-watch/traffic-app/dist/map_tiles/;
        add_header Cache-Control "public, max-age=2592000, immutable";
    }

    location /fonts/ {
        alias /home/ubuntu/projects/san-diego-traffic-watch/traffic-app/dist/fonts/;
        add_header Cache-Control "public, max-age=2592000, immutable";
    }

    location / {
        proxy_pass http://127.0.0.1:5002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300;
    }
}
